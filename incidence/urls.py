from django.urls import path, re_path
from django.contrib.auth.models import User
from rest_framewrork import router, serializers, viewsets
from . import views

app_name = 'incidence'


urlpatterns = [
	# re_path(r'^$', )
	
]